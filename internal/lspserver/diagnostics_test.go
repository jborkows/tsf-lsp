package lspserver

import (
	// "fmt"
	"testing"
	// "math/rand/v2"
)

func Test_if_free_it_should_be_only_it(t *testing.T) {

	// freeDays := FreeDays().FreeDaysCategories()
	// freeDay := freeDays[rand.IntN(len(freeDays))]
	// not_valid := fileContentFromText(fmt.Sprintf("%s\nSthEle\nSthElse2", freeDay))
	//
	// diagnostics := getDiagnosticsForFile(&not_valid)
	//
	// assert.Equal(t, len(diagnostics), 2, "Should only have single line")
}
